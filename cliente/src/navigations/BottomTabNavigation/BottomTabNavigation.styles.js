import { StyleSheet } from "react-native";

export const styles =new StyleSheet.create({
    tabBarStyle: {
        borderTopWidth: 1,
        height: 62,
        paddingTop: 6,
        paddingBottom: 8,
    },
});
